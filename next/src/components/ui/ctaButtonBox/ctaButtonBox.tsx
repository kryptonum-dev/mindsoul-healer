import styles from './ctaButtonBox.module.scss';
import type { ctaButtonBoxTypes } from './ctaButtonBox.types';

export default function CtaButtonBox({  }: ctaButtonBoxTypes) {
  return (
    <div className={styles['CtaButtonBox']}>

    </div>
  );
}